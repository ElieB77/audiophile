import styles from "./styles.module.scss";

const CartModal = () => {
  return (
    <div className={styles.__cart_modal}>
      <div className={styles.__cart}>Cart</div>
    </div>
  );
};

export default CartModal;
